---
import Layout from "../../layouts/Layout.astro";
import { getAllCareers } from "../../lib/sanity";
import { toHTML } from "@portabletext/to-html";

const careers = await getAllCareers();
---

<Layout title="Perjalanan Karir - Rafi Romolo">
  <section id="career-detail">
    <a href="/#karir" class="back-link">&larr; Kembali ke Homepage</a>

    <p class="eyebrow">Pengalaman</p>
    <h1>Perjalanan Karir</h1>
    <p class="intro">
      Rangkuman lengkap pengalaman kerja saya, dari awal karir sampai posisi
      saat ini.
    </p>

    <ol class="timeline">
      {
        careers.map((entry) => (
          <li class="timeline-item">
            <div class="marker">
              <span
                class:list={["dot", { current: entry.isCurrent }]}
                aria-hidden="true"
              />
            </div>
            <div
              class:list={[
                "card",
                "entry-card",
                { "entry-card--current": entry.isCurrent },
              ]}
            >
              <div class="entry-head">
                <h2>{entry.role}</h2>
                {entry.isCurrent && <span class="badge-current">Saat ini</span>}
              </div>
              <p class="company">{entry.company}</p>
              <p class="period">{entry.period}</p>
              {entry.summary && (
                <p class="summary" set:html={toHTML(entry.summary as any)} />
              )}

              {entry.details && entry.details.length > 0 && (
                <ul class="details">
                  {entry.details.map((detail) => (
                    <li set:html={toHTML(detail as any)} />
                  ))}
                </ul>
              )}
            </div>
          </li>
        ))
      }
    </ol>
  </section>
</Layout>

<style is:global>
  #career-detail h1 {
    font-size: 2.5rem;
  }

  #career-detail .back-link {
    display: inline-block;
    margin-bottom: 2rem;
    color: var(--color-muted);
    font-size: 0.9rem;
  }

  #career-detail .eyebrow {
    margin: 0 0 0.4rem;
  }

  #career-detail .intro {
    color: var(--color-muted);
    max-width: 60ch;
    margin: 0 0 3rem;
  }

  #career-detail .company {
    font-weight: 600;
    margin: 0.35rem 0 0.15rem;
  }

  #career-detail .period {
    font-size: 0.85rem;
    color: var(--color-muted);
    margin: 0 0 1rem;
  }

  #career-detail .summary {
    color: var(--color-muted);
    margin: 0 0 1rem;
  }

  #career-detail .summary p {
    margin: 0;
  }

  #career-detail .details {
    margin: 0;
    padding-left: 1.25rem;
    color: var(--color-muted);
    display: grid;
    gap: 0.5rem;
  }

  #career-detail .details li::marker {
    color: var(--grad-2);
  }

  #career-detail .details li p {
    margin: 0;
  }
</style>
