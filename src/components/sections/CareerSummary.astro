<div id="container">
  <main>
    <section id="karir">
      <div class="blob" aria-hidden="true"></div>
      <div class="karir-heading">
        <div>
          <p class="eyebrow">Pengalaman</p>
          <h2>Perjalanan Karir</h2>
        </div>
        <a class="view-all" href="/career">
          Lihat Semua <span aria-hidden="true">&rarr;</span>
        </a>
      </div>
      <ol class="timeline">
        <li class="timeline-item">
          <div class="marker">
            <span class="dot current" aria-hidden="true"></span>
          </div>
          <div class="card entry-card entry-card--current">
            <div class="entry-head">
              <h3>Digitalisasi Finance Accounting</h3>
              <span class="badge-current">Saat ini</span>
            </div>
            <p class="company">PT Mekar Armada Jaya</p>
            <p class="period">Sep 2024 - sekarang</p>
            <p class="summary">
              Membangun dan memelihara aplikasi web dari sisi frontend hingga
              sedikit backend, fokus pada pengalaman pengguna yang clean dan
              performa yang cepat.
            </p>
          </div>
        </li>
        <li class="timeline-item">
          <div class="marker"><span class="dot" aria-hidden="true"></span></div>
          <div class="card entry-card">
            <div class="entry-head"><h3>IT</h3></div>
            <p class="company">CV Prima Print</p>
            <p class="period">Apr 2024 - Jul 2024</p>
            <p class="summary">
              Deskripsi singkat pengalaman kerja sebelumnya dan hal relevan yang
              kamu kerjakan di sana.
            </p>
          </div>
        </li>
      </ol>
    </section>
  </main>
</div>

<style>
  #karir {
    position: relative;
    overflow: hidden;
  }

  .blob {
    position: absolute;
    width: 380px;
    height: 380px;
    background: var(--gradient);
    border-radius: 50%;
    filter: blur(90px);
    opacity: 0.15;
    top: -10%;
    left: -10%;
    z-index: 0;
    pointer-events: none;
  }

  .karir-heading {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 2.5rem;
    flex-wrap: wrap;
  }

  .karir-heading .eyebrow {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 0.85rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    background: var(--gradient);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    margin: 0 0 0.4rem;
  }

  .karir-heading .view-all {
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--color-text);
    white-space: nowrap;
    padding-bottom: 0.3rem;
  }

  .company {
    font-weight: 600;
    margin: 0.3rem 0 0.15rem;
  }

  .summary {
    color: var(--color-muted);
    margin: 0;
  }

  @media (max-width: 600px) {
    .timeline-item {
      grid-template-columns: 1.5rem 1fr;
      gap: 0.85rem;
    }
  }
</style>
